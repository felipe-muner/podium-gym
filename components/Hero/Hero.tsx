import React from "react";

const Hero: React.FC = () => {
  return (
    <section className="bg-brand-background-1 pb-[70px] pt-[100px] w-full">
      hero
    </section>
  );
};

export default Hero;
